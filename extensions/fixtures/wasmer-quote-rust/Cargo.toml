[package]
name = "rustashop-wasmer-quote-rust"
description = "Wasmer sandbox Rust WASI quote(cart) fixture"
version = "0.1.0"
edition = "2024"
license = "OSL-3.0"
authors = ["Interchouette"]
publish = false

[workspace]

[[bin]]
name = "quote"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
