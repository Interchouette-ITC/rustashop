# wasmer-quote-rust fixture

Rust WASI guest for the Wasmer polyglot sandbox harness (`rustashop-sandbox`).

Same JSON ABI as `extensions/fixtures/wasmer-quote/quote.py`: cart on stdin, adjustments on stdout.

## Rebuild

```bash
make sandbox-quote-rust-fixture
```
