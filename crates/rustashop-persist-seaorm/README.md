# rustashop-persist-seaorm

SeaORM path: entities and migrations that mirror the SQLx schema, plus the same
repository surface as the SQLx adapter.

Selected through `rustashop-persist` with `--no-default-features --features persist-seaorm`.
Binary: `rustashop-seaorm-migrate`.
