# rustashop-domain

Pure commerce domain types and logic: money (integer minor units), catalog,
cart, checkout, and orders. No ORM or database types.

Persistence adapters and HTTP DTOs live in sibling crates.
