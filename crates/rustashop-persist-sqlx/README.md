# rustashop-persist-sqlx

SQLx (no ORM) path: versioned SQL migrations, parameterized queries, and
repository implementations for catalog, cart, checkout, and orders.

Selected through `rustashop-persist` with the default `persist-sqlx` feature.
Binary: `rustashop-migrate`.
