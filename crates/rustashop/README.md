# rustashop

Application kernel package: boots a Serenade `App` with `FrameworkBundle` and
`RustashopBundle`, then builds the DI container from `config/packages`.

Commerce HTTP lives in `rustashop-api`. Persistence is selected via
`rustashop-persist`.
